import React from 'react';
import App from '@/App';

export default function IndexPage() {
  // Render the SPA App component so client-side routing (react-router) works under Next
  return <App />;
}
