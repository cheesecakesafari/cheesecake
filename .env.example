# Example environment variables for local development
# Copy this file to `.env` and fill in any secrets. DO NOT commit real secrets.

# Frontend API base used by Vite (where backend server runs)
VITE_API_BASE=http://localhost:5000

# If you run the comments/gallery backend (backend/index.js) it reads DATABASE_URL
# Example (do NOT store real creds here):
# DATABASE_URL=mongodb+srv://<user>:<pass>@cluster0.mongodb.net/cheesecake_comments?retryWrites=true&w=majority

# If you use the alternative server at server/index.js, set these instead:
# MONGODB_URI=
# MONGODB_DB=cheesecake
